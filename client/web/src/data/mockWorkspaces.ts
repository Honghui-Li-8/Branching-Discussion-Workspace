import type { Workspace } from '../types/workspace'

export const initialWorkspaces: Workspace[] = [
  {
    id: 'workspace-1',
    title: 'Project Decision (dummy example)',
    summary: 'Should I build the workspace in the next 4 weeks?',
  },
]
